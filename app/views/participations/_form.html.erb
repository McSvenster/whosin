<%= form_for(@participation) do |f| %>
  <% if @participation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participation.errors.count, "error") %> prohibited this participation from being saved:</h2>

      <ul>
      <% @participation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<h2><%= @participation.milestone.project.title + " : " + @participation.milestone.title + " : " + @participation.user.vname %></h2>
  </div>

  <div class="field">
    <%= f.label :startdate %><br>
    <%= f.date_field :startdate %>
  </div>
  <div class="field">
    <%= f.label :enddate %><br>
    <%= f.date_field :enddate %>
  </div>
  <div class="field">
    <%= f.label :attention %><br>
    <%= f.number_field :attention %>
  </div>
  <div class="field">
    <%= f.label :peak_id %><br>
    <%= f.number_field :peak_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
