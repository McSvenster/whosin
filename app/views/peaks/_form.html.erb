<%= form_for(peak) do |f| %>
  <% if peak.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peak.errors.count, "error") %> prohibited this peak from being saved:</h2>

      <ul>
      <% peak.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :peaksign %>
    <%= f.text_field :peaksign %>
  </div>

  <div class="field">
    <%= f.label :peakformula %>
    <%= f.text_field :peakformula %>
  </div>

  <div class="field">
    <%= f.label :peakimage %>
    <%= f.text_field :peakimage %>
  </div>

  <div class="field">
    <%= f.label :participation_id %>
    <%= f.number_field :participation_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
