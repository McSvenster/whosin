<tr>
  <td class="top"><%= link_to "#{user.vname} #{user.nname}", edit_user_path(user) %><br>
  &nbsp;<%= user.email %></td>
  <td>
    <%= form_for(user, remote: true) do |u| %>
      <%= u.text_area :blockdaten, value: user.blockdaten, onchange: "$(this).parent('form').submit();" %>
    <% end %>
  </td>
</tr>