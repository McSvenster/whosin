<%= form_for @plan do |f| %>
  <%= f.fields_for :attendances, [Attendance.new]*(@users.size) do |att| %>
    <%= att.collection_select(:user_id, @users, :id, :vname, {prompt: "please select"}) %><br>
  <% end %>
  <%= f.submit %>
<% end %>